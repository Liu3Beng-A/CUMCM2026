---
description: CUMCM 2026 E 题项目事实索引——路径、算法、方案、数据规模、运行命令；含强制流程与附件 2 模板约束
globs:
alwaysApply: true
---

# CUMCM 2026 E 题 · SEM 广告投放策略优化 · 项目事实索引

> 本文件是本仓库的事实清单：文件在哪、用了什么算法/方案、数据是多少、怎么运行。
> 不含评价、评级、原因、边界、建议。

---

1. **读题面图**（page 1 + page 2）：
   - `paper/figures/raw_attachments/problem_pages/page_1.png`
   - `paper/figures/raw_attachments/problem_pages/page_2.png`
   - OCR 源（备）：`paper/figures/raw_attachments/e_images/page_1.png`, `page_2.png`

2. **读附件 1 数据**（3 sheet）：
   - `data/raw/attachments/附件1.xlsx`
   - Sheet1 = 方案日表（日期, 方案ID, 推广单元ID, 展现/点击/消费/上方位）
   - Sheet2 = 新注册表（日期级）
   - Sheet3 = 关键词表（序号, 关键词, 方案ID, 推广单元ID, 消费额, 点击量, 浏览量, 跳出率, 平均访问时长）

3. **读附件 2 模板**（**最关键、最容易遗漏**）：
   - `data/raw/attachments/附件2/result2.xlsx` ← Q2 输出格式
   - `data/raw/attachments/附件2/result3.xlsx` ← Q3 输出格式
   - `data/raw/attachments/附件2/result4.xlsx` ← Q4 输出格式
   - **不要只看列名，必须读出空模板的具体行结构 + 决定输出粒度**

4. **跑题前先列：题面要求 vs 模板约束 vs 我打算做什么** —— 三者必须一致才能开始写代码。

### 第二动作：自动日志写入（**每完成一个动作立即追加**，不得积压）

> **强制规则（2026-09-12 新增）**：每完成一个动作（不论多小：备份 / 删除 / 修改 / 跑脚本 / 生成图表 / 文档同步 / 与用户确认 / 备份后再删 / 关键判断等），
> **必须在 `RUN_LOG.md` 末尾立即追加一行**，格式严格遵守：
