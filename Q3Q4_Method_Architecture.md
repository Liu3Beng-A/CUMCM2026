# Q3/Q4 方法架构 · Method Architecture

> 起草时间：2026-09-12 14:50
> **PoC 修订：2026-09-12 16:42（详见 `PoC_REPORT.md`）**
> **BIAS 修订：2026-09-12 16:57（详见 `BIAS_REPORT.md`，10 项偏差）**
> 起草人：第 N 轮 agent（衔接 Q2 实施规划）
> 适用对象：负责实现 Q3 / Q4 的下一任 agent
> 目标：在**严格对齐附件 2 模板** + **不偏离题面**的前提下，给出既能"国奖评审过关"又**不让上级再次发生 hallucination** 的方法架构
> 严正声明：本文档由上级在用户授权下撰写；执行 agent 须按本文档方法落地，**禁止自行扩展为更复杂的方法体系（NSGA-II/DRL/GAN 等）**

---

## ⚠️ 执行 agent 必须先读的三件事

1. `issue/q2_implementation_plan.md` —— Q2 已锁定的 5 项决策（D-Q2-001 ~ D-Q2-005），是 Q3/Q4 的输入约束
2. `issue/q1_fix_plan.md` 头部 —— 了解 P0-0 节假日修正 + Q1 评分体系
3. **`PoC_REPORT.md`** —— 2026-09-12 16:42 数据探测结论（**PoC 已验证**，影响实现细节）
4. **本文件** §1 数据契约 + §2/3 方法架构 + §5 红线

执行前**严禁**：
- 跳过第 1 步读附件 2 模板
- 写"我们用 Transformer/Attention/GAN…"等堆砌型方法
- 修改 Q1 已确定的评分函数 / 权重 / 假日扣分

---

## 0. Q2 方案分析方式总结（给执行 agent 的导读）

| 项 | 决定 | 含义 / 后果 |
|---|---|---|
| 输入 | Sheet3 关键词级 9 列数据 | "成本"用消费额、"效益"用 CPC 倒数 |
| 主分类器 | 二维硬阈值（中位数）+ 5 类 one-hot | 严禁用聚类（GMM/KMeans）|
| 无效词规则 | 消费=0 即无效（890 词）| 与"无成本、无效益"题面语义一致 |
| 行粒度 | **2227 行明细 one-hot** | 每关键词 1 行，5 类离散列 |
| 副输出 | 60 行推广单元聚合 | 跨方案对比 |
| 校验 4 项 | 阈值敏感性龙卷风 + Bootstrap CI + 跨方案对比 + 可选 CRITIC | 国奖评审加分项 |
| 极值审计 | 112 词 Top 高消费 + 风险评级 | heavy-tail 修正 |

**Q3 必须消费 Q2 的 4 个产物**：
- `data/processed/q2/keyword_classified.pkl`（含 5 类 one-hot + 5 维特征）
- `results/tables/q2_thresholds.json`（阈值可复用）
- `results/excel/result2.xlsx`（公式读取）
- `results/tables/q2_extreme_audit.csv`（极值标签）

---

## 1. 数据契约（**直接来自附件 2 模板 + 附件 1 Sheet 列**，不是 mdc 摘要）

### 1.1 输入资源

| 资源 | 路径 | 关键列（**真实列名**）|
|---|---|---|
| Sheet1 方案日表 | `data/raw/attachments/附件1.xlsx` Sheet1 | **日期, 方案ID, 推广单元ID, 展现量, 点击量, 消费额, 上方位展现量, 上方位首位展现量, 上方位点击量, 上方位消费额**（10 列，2627 行；**日级**）|
| Sheet2 新注册日表 | `data/raw/attachments/附件1.xlsx` Sheet2 | **日期, 新注册数**（**注意列名尾部有空格**，必须 `df.columns.str.strip()`；2 列，365 行；**日级**）|
| Sheet3 关键词表 | `data/raw/attachments/附件1.xlsx` Sheet3 | **序号, 关键词, 方案ID, 推广单元ID, 消费额, 点击量, 浏览量, 跳出率, 平均访问时长**（9 列，2227 行；**⚠️ 关键词级全年累计口径，非日级！详见 §1.4**）|
| Q2 5 类分类 | `results/excel/result2.xlsx` | **方案ID, 推广单元, 序号, 黄金词, 重点词, 潜力词, 问题词, 无效词**（**BIAS 修订**：列名带"词"字；8 列，2227 行）|
| Q2 阈值参数 | `results/tables/q2_thresholds.json` | T_cost, T_benefit, n_per_class |
| Q2 极值审计 | `results/tables/q2_extreme_audit.csv` | ~112 词 + 风险评级 |

> **PoC 验证证据**（2026-09-12 16:42，详见 `PoC_REPORT.md`）：
> - Sheet3 消费额合计 = 1,425,949.81 元 ≈ Sheet1 全年合计 = 1,425,949.79 元（差 < 0.01%）→ Sheet3 是"关键词级全年累计"
> - Sheet3 无"展现量"列，890 词（40%）消费=0 且 跳出率/访问时长="/"，与 Q2 无效词完全重合
> - 16 天（2025-02-01~08 + 08-01~08）推广单元-日 112 条，总预算 51,164.93 元，单元-日均 max=2,237.33 / min=1.58（差异 1400×）|

### 1.2 输出契约（**严格对齐附件 2 result{3,4} 模板 9 列**）

| 列位 | 模板列名 | 类型 | 含义 / 口径 |
|---|---|---|---|
| 1 | **日期** | string `YYYY-MM-DD` | 投放日 |
| 2 | **方案ID** | int | 与 Sheet1 一致 |
| 3 | **推广单元** | int | 与 Sheet1 一致（注意：模板是"推广单元"非"推广单元ID"，列名严格遵循）|
| 4 | **关键词** | string | 与 Sheet3 一致 |
| 5 | **投入金额** | float（元）| 该日对该关键词的预算 |
| 6 | **预期展位** | int | **口径 = 上方位展现量**（Sheet1 列 6，top-position impressions；**BIAS 修订**：占总展现量 27%，详见 §1.2.1）|
| 7 | **预期点击量** | int | 口径 = 该关键词该日的总点击量 |
| 8 | **预期浏览量** | int | 口径 = 该关键词该日的总浏览量（Sheet3 浏览量按关键词-天缩放）|
| 9 | **预期注册量** | float | 口径 = 该关键词该日期望的新注册数（保留小数）|

> **关键口径**（写死，不允许改）：
> - result3 时间范围：**2025-02-01 ~ 02-08 + 2025-08-01 ~ 08-08 = 16 天**
> - result4 时间范围：**2026-09-11 ~ 09-17 = 7 天**
> - Q3 行粒度：`推广单元 × 关键词 × 天`（至少 1 行 = 1 个投放组合，不允许"方案级聚合掉所有关键词"）
> - Q4 行粒度：同 Q3，但只覆盖 2026-09-11~17
> - 入选词集合：**Q2 5 类中"黄金 + 重点 + 潜力"**（问题/无效淘汰）

### 1.2.1 "预期展位"口径选择（**BIAS 修订**）

| 候选口径 | 数据来源 | 占比 | 评估 |
|---|---|---|---|
| **上方位展现量（top-position impressions）** | Sheet1 列 6 | 27.11% (6.12M / 22.58M) | ✅ **本文采用**——与 Sheet1 列名匹配 |
| 上方位首位展现量 | Sheet1 列 7 | 10.39% (2.35M / 22.58M) | 备选，更严格但印数偏小 |
| 平均展现位 rank | 无 Sheet1 列 | - | ❌ 不可行（无原始数据）|
| 展现量（总曝光） | Sheet1 列 3 | 100% | 备选，但语义偏宽 |

**采用上方位展现量的理由**：
1. Sheet1 实际列名（"上方位展现量"）与模板"预期展位"语义接近
2. 占总展现量 27%，符合"top-position"的业务直觉
3. 论文 §5.3 必须显式说明此选择，并给出"上方位展现量/展现量 = 27%"的对比表

### 1.3 行数预期（执行 agent 自检用）

| 输出 | 预期行数 | 说明 |
|---|---|---|
| result3.xlsx | **5,000~20,000 行** | 16 天 × 入选项 × 推广单元（每天每推广单元选 topK 关键词）|
| result4.xlsx | **1,000~5,000 行** | 7 天 × 入选项 × 推广单元 |
| 关键词关联图 | ~300 节点 + ~1000 边 | 同推广单元下共现 + 同时段同时展现 |

### 1.4 时间粒度标准化（**PoC 新增，必读**）

| 数据源 | 时间粒度 | 备注 |
|---|---|---|
| Sheet1 (方案日) | **日级**（2627 行 = 365 天 × ~7.2 行/天）| `日期, 方案ID, 推广单元ID` 3 维定位 |
| Sheet2 (新注册) | **日级**（365 行）| 仅 `日期` 1 维 |
| Sheet3 (关键词) | **全年累计**（2227 行 = 1 行/关键词）| **无日期列**，需通过 §2.2.1 代理变量法分配到日 |
| Q2 5 类分类 | **关键词级**（继承 Sheet3，全年累计）| 1 行/关键词 |

**统一基准（写死）**：所有"日级"指标用 Sheet1 实际值；所有"关键词级"指标用 Sheet3 全年值按"日消费÷全年消费"分配。

**入选项预估（Q3 决策变量上界）**：
- 入选项 = 黄金(431) + 重点(238) + 潜力(240) = **909 词**（占 2227 = 40.82%）
- 16 天 × 909 词 = 14,544 个 `(d, k)` 组合（**BIAS 修订**：决策变量已从 (d,p,k) 简化为 (d,k)，详见 §2.3）
- MILP 剪枝后实际行数约 5K-10K（仍符合 §1.3 5K-20K 预估）

**Q3 预算分段（**BIAS 新增**：必须分 02/08 月段，不能合并**）：
| 时段 | 总消费 | 日均 | 备注 |
|---|---|---|---|
| **02-01~08**（春节假期）| **13,681.15 元** | 1,710.14 元 | 投放大幅减少 |
| **08-01~08**（暑假）| **37,483.78 元** | 4,685.47 元 | 正常投放期 |
| 合计 | 51,164.93 元 | 3,197.81 元 | 与 PoC 一致 ✓ |

→ 推广单元-日预算范围：1.58 ~ 2,237.33 元（差异 1400×）→ **必须按单元设预算上限**

---

## 2. Q3 方法架构

### 2.0 问题定性（**BIAS 修订**：消除"预测"歧义）

> **关键**：Q3 时间范围 = `2025-02-01 ~ 02-08 + 2025-08-01 ~ 08-08` 是 **2025 年已经发生过的 16 天**。
>
> Q3 的本质 = **历史反事实优化**（给定预算约束 + 关键词池 + 关联规则，回顾性找最优投放决策，并估算若按此策略投放的预期产出）。
>
> **不是未来预测**（2026 年才是未来预测，归 Q4）。

**与 Q4 的对比**：

| 维度 | Q3 | Q4 |
|---|---|---|
| 时间 | 2025-02-01~08 + 08-01~08 (历史) | 2026-09-11~17 (未来) |
| 性质 | **反事实优化 + 历史回放** | **未来预测 + 不确定性建模** |
| 产出 | 最优投放决策 + 预期产出估算 | 最优投放决策 + 期望值 + CI |
| 同期预算 | 2025 同期 = 51,164.93 元（已发生）| 2025 同期 = 23,488.02 元（参考上限）|
| 扰动 | 无（确定性 MILP）| 6 因子扰动（Two-Stage SP）|

### 2.1 设计原则（5 条，继承 Q2 + mdc v3 + BIAS 修订）

1. **题面第一**：Q3 题面原文 4 个元素（关键词类型 + 关联关系 + 低成本高效益 + 不超预算），方法必须显式回答这 4 个元素
2. **不是黑盒**：每个公式必须能在论文 §5.3 用 1 行 LaTeX 描述
3. **可解释 > 准确**：国奖评审更看重"为什么这么做"，不是 RMSE 跑分
4. **预算约束硬**：日预算 ≤ 2025 同期日均（不是日均 × N 倍系数）
5. **（BIAS 新增）投放模型 = 三层表达**：（a）MILP 数学公式（§2.3）+（b）业务规则摘要（黄金/重点/潜力入选，问题/无效淘汰）+（c）关联规则集合（FP-Growth 输出）。论文 §5.3 须显式表达这三层。

### 2.2 方法栈（4 层，**严格按从底向上**）

#### Layer 1：关键词 5 维特征工程（继承 Q2）

输入：Sheet3 2227 词 + Q2 分类结果。

对每个关键词计算 5 维特征：
- **成本维度**：消费额（log 标度处理 heavy-tail；**注：是 Sheet3 全年累计值**）
- **效益维度**：CPC 倒数 = 点击量 / max(消费额, 0.01)
- **流量维度**：浏览量（按关键词级别，**全年累计**）
- **质量维度**：1 - 跳出率（"无数据"/'/' 值填 = 推广单元均值或 1.0 中性；890 词无效词天然为 "/"）
- **粘性维度**：平均访问时长（转秒数）

输出：`data/processed/q3/keyword_5d.pkl`

#### Layer 1.5：代理变量法（**PoC 新增，关键**）

> **问题**：模板要求每行 = (日期 × 推广单元 × 关键词) 的 4 指标预测，但 Sheet3 只有"关键词级全年累计"，无日级数据。
>
> **解决**：用 Sheet1 推广单元-日级 指标 × 关键词级消费占比 推到 关键词-日级。

**代理公式（写死，不允许改）**：

```
kw_day_消费  = unit_day_消费  · (kw_全年消费 / unit_全年消费)        # 分配权重
kw_day_点击  = kw_day_消费   · (kw_全年点击 / kw_全年消费)           # 用历史 CPC
kw_day_浏览  = kw_day_消费   · (kw_全年浏览 / kw_全年消费)           # 用历史 b/C 比
kw_day_展位  = unit_day_上方位 · (kw_day_消费 / unit_day_消费)        # 展位与消费强相关
kw_day_注册  = unit_day_注册 · (kw_day_消费 / unit_day_消费)          # 代理（最弱）
```

**注册分配链路**（两步）：
1. Sheet2 (日注册) × (unit_day_消费 / day_总消费) → 推广单元-日注册
2. 推广单元-日注册 × (kw_全年消费 / unit_全年消费) → 关键词-日注册

**关键假设（论文 §5.3.2 必须显式声明）**：
- 假设 1：同一推广单元内，关键词级消费占比在 16 天内近似稳定（用全年比例代理日级）
- 假设 2：浏览量、注册量与消费额线性相关（弱假设，需敏感性分析）

#### Layer 2：关键词关联挖掘（**国奖关键**，FP-Growth）

**目标**：发现"同一推广单元下同时投放的有效组合"，回答题面"考虑关键词类型和关联关系"。

**算法（FP-Growth，不是 GMM！FP-Growth 是数据挖掘经典关联规则算法）**：
- 输入：每天每个推广单元实际投放的关键词集合（从 Sheet1 重构）
- 项集：关键词 ID（按"方案-推广单元"分组 daily）
- 最小支持度：min_support = 5%（即至少在 5% 推广单元日中出现）
- 最小置信度：min_confidence = 60%
- 输出：高频关键词组 + 关联规则 A → B（支持度 / 置信度 / 提升度）

**业务价值**：
- "黄金 + 潜力" 联动投放可提升整体 ROI
- 同一推广单元下高频共现词可降低单次点击成本（CPC 分摊）
- 在 Q3 投放策略中作为"种子词 + 扩展词"机制使用

输出：
- `results/tables/q3_keyword_assoc_rules.csv`（关联规则：前项 → 后项 | sup | conf | lift）
- `results/figures/q3_keyword_assoc_network.png`（关联网络图，节点 = 关键词，颜色 = Q2 分类，边粗细 = lift）

#### Layer 3：16 天预算分配与关键词选择（**Mixed-Integer Linear Programming**）

> 选择 MILP 的理由：题面"选择合适的关键词"+"保证不超预算"+"低成本高效益" = 标准 MILP 语义；可解释；与"国奖不堆方法"原则一致

**决策变量（**BIAS 修订**：从 (d,p,k) 简化为 (d,k)，因为 k 唯一确定 (方案, 推广单元)）**：
- `x[d, k]` ∈ {0, 1}：第 d 天是否投放关键词 k（0/1 变量，**关键 discrete**）
- `y[d, k]` ≥ 0：第 d 天对关键词 k 的投入金额（连续变量，单位 = 元）
- **注**：关键词 k 的 (方案ID, 推广单元ID) 通过 Sheet3 一次性 lookup → 写入模板时填入对应方案ID/推广单元列
- **规模缩减**：16 天 × 909 词 = 14,544 决策变量（原 174,528 → 减 92%）
- **推广单元预算约束**改为：`∑_k y[d, k] · 1{k ∈ unit p} ≤ B_unit[p, d]`


**目标函数（**PoC 修订**：原版除以 y 在 y→0 时数学漏洞，已修正为 LP 形式）**：

```python
# === 原版（BUG：y→0 时收益 → ∞）===
# max  ∑ (α·E_click + β·E_browse + γ·E_reg) / max(y[d,p,k], 0.01)

# === 修正版（标准 LP，线性）===
# 定义历史比值（常量，由 §2.2.1 代理变量法预计算）：
r_click = kw_全年点击 / kw_全年消费      # 单位消费点击数
r_browse = kw_全年浏览 / kw_全年消费     # 单位消费浏览数
r_reg    = kw_全年注册 / kw_全年消费     # 单位消费注册数（代理变量法计算）

# 目标函数（线性 LP，**BIAS 修订**：决策变量从 (d,p,k) 改为 (d,k)）：
max  ∑_{d,k}  (α·r_click + β·r_browse + γ·r_reg) · y[d, k]
    + δ · 关联应用率      # 关联软奖励（δ = 0.05，小权重）
s.t. ∑ y ≤ B_total
```

**权重（PoC 推荐）**：
- α = 0.50（点击主目标，转化漏斗上层）
- β = 0.30（浏览，流量质量）
- γ = 0.20（注册，终极目标）
- δ = 0.05（关联软奖励）
- 论文 §5.3.2 必须给出 3 档敏感性对比（α 占 0.6/0.5/0.4）

**约束（5 条硬约束 + 1 条软约束）**：

1. **入选项选择**（硬）：仅 Q2 的"黄金+重点+潜力"词可在 x=1
   ```
   x[d, k] = 0    ∀ k ∈ {问题词, 无效词}, ∀ d
   ```
   （**BIAS 修订**：从 (d,p,k) 简化为 (d,k)，更直接表达"问题/无效词永不投放"）
2. **推广单元日预算**（**BIAS 修订**：决策变量简化后改写）
   ```
   ∑_k y[d, k] · 1{k ∈ unit p} ≤ B_unit[p, d]    ∀ d, p
   ```
   其中 `B_unit[p, d]` = 推广单元 p 在 2025 同期 (2025-02-01~08 对应 02-01~08，2025-08-01~08 对应 08-01~08) 的实际日消费（**PoC 验证**：单元差异 1400×，单元-日均 max=2,237.33 元 / min=1.58 元）
3. **总预算**（**BIAS 修订**：分段 02/08 月，不可合并）：
   ```
   ∑_{d ∈ Feb} y[d, k] ≤ 13,681.15 元       (02-01~08, 春节段)
   ∑_{d ∈ Aug} y[d, k] ≤ 37,483.78 元       (08-01~08, 暑假段)
   ```
   **理由**：02 月段（春节假期，投放大幅减少）vs 08 月段（暑假，投放正常）的预算级别差 ~3 倍，合并会误导优化方向。
   两段合计 = 51,164.93 元（**PoC 实测值**）
4. **x-y 联动**（**BIAS 修订**：决策变量简化）：
   ```
   y_min · x[d,k] ≤ y[d,k] ≤ y_max · x[d,k]
   ```
   y_min = 1 元，y_max 由推广单元预算约束隐式给出
5. **问题/无效词硬淘汰**（硬）：若 k ∈ {问题词, 无效词} 则 x[d,p,k] = 0
6. **关联规则**（**PoC 修订**：硬约束→软约束，目标函数奖励）
   - ❌ 删除原"conf>0.8 则 A 投放 → B 必须投放"硬约束（可能导致 infeasible 环）
   - ✅ 改为：计算关联应用率 = 满足 conf>0.8 规则且 x[d,p,A]=x[d,p,B]=1 的组合数 / 总规则数，加入目标函数 `+ δ · 应用率`

**infeasible fallback（PoC 新增）**：
```
if solver.status == infeasible:
    1. 松弛约束 4（y_min = 0.5 → 0.1）
    2. 松弛约束 6（δ 失效 → 应用率 ≥ 30% 即可）
    3. 仍 infeasible → 禁用关联约束，重解（关联应用率作为论文指标而非硬约束）
```

**求解器**：
- 首选 PuLP（纯 Python，易调试，**不要用 CPLEX/Gurobi 商业授权**）
- 备选 OR-Tools（如果 PuLP 性能不够）
- 不接受 NSGA-II/遗传算法/强化学习（**国奖不堆方法**）

**关键简化**（避免维度爆炸）：
- 按推广单元分 12 个独立子问题（MILP 松弛规模从百万降到千级）
- 每个推广单元独立优化后再做"全局 sum 不超预算"的可行性校验（必要时缩放各单元解）

输出：
- `results/tables/q3_daily_strategy.csv`（16 天 × 12 推广单元 × 入选项）
- `results/excel/result3.xlsx`（按 row-level 模板格式输出）
- `data/processed/q3/milp_solution.pkl`（含决策变量，便于反事实）

#### Layer 4：四指标反事实估算（**确定性估算，BIAS 修订**：Q3 是历史回放不是未来预测）

> **基线**：本层基于 §2.2.1 代理变量法预计算的 4 个历史比值（`r_click / r_browse / r_reg / r_imp_top`），对每个被选中的 (d, k) 组合（x[d,k]=1）做**反事实估算**（即"如果 2025 年 16 天按 MILP 解投放，预期产出多少"）。

**4 指标预测公式（**PoC 修订**：全部线性，依赖 §2.2.1 的代理比值**）**：

| 指标 | 公式 | 输入 / 来源 |
|---|---|---|
| 预期展位 | `y[d,p,k] · r_imp_top` | `r_imp_top` = kw_全年消费占比 × unit_day_上方位 ÷ kw_消费（代理变量法）|
| 预期点击量 | `y[d,p,k] · r_click` | `r_click` = kw_全年点击 ÷ kw_全年消费 |
| 预期浏览量 | `y[d,p,k] · r_browse` | `r_browse` = kw_全年浏览 ÷ kw_全年消费 |
| 预期注册量 | `y[d,p,k] · r_reg` | `r_reg` = unit_day_注册 × kw_消费占比 ÷ kw_消费（两步代理）|

**比值来源（PoC 实测口径）**：
- 单位消费上方位展现量：从 Sheet1 推广单元-日序列 16 天窗口取算术平均
- 单位消费点击量/浏览量：从 Sheet3 关键词全年累计（消费>0 的 1,337 词）
- 单位消费注册数：Sheet2 日注册 → 推广单元-日 → 关键词级（两步代理，最弱）

> **简化但有效**：所有比值用"2025 同窗口"（2025-02-01~08 与 2025-08-01~08）实际数据算术平均，作为常数；不做时间序列外推（理由：题面只 16 天，历史太短做 ARIMA/Prophet 没意义）

**鲁棒性修正**（避免 overconfidence）：
- 对每个比值乘以 0.85 折扣（防系统性高估）
- 对极端值做 95th 截尾（防 heavy-tail 偏倚）
- 提供"上界 vs 下界"两套预测，给论文 §5.3.4

输出：
- `results/excel/result3.xlsx`（4 个预期列 + 投入金额）
- `results/tables/q3_indicator_summary.csv`（4 指标总量统计）

### 2.3 校验机制（4 项，**国奖评审加分**）

| 校验 | 做法 | 通过标准 |
|---|---|---|
| **预算守门** | sum(投入金额) ≤ 2025-02+2025-08 实际消费 | 严格 ≤（不留 buffer） |
| **历史回放** | 用 2025 全年数据做 backtest：把 Q3 的投入金额方案套到 2025 实际产出，看是否接近实际 | 预测点击数 / 实际点击数 ∈ [0.85, 1.15] |
| **入选项准入** | 100% 入选词 ∈ 黄金∪重点∪潜力，0% 包含问题或无效词 | 严格相等 |
| **关联应用率** | 关联规则中"强制共现"的覆盖率（即 满足 conf>0.8 规则的词组比例）| ≥ 60% |

### 2.4 输出物清单（Q3 实施 agent 交付）

| 产出 | 路径 | 行/字段数 |
|---|---|---|
| result3.xlsx（主交付）| `results/excel/result3.xlsx` | 5K-20K 行 × 9 列 |
| 5 维特征 pkl | `data/processed/q3/keyword_5d.pkl` | 2227 × 5+ |
| 关联规则 csv | `results/tables/q3_keyword_assoc_rules.csv` | ~100-200 行 |
| 关联网络图 png | `results/figures/q3_keyword_assoc_network.png` | 1 张 |
| MILP 求解 pkl | `data/processed/q3/milp_solution.pkl` | 16 × 12 × K |
| 每日策略 csv | `results/tables/q3_daily_strategy.csv` | 同 result3.xlsx |
| 4 指标汇总 | `results/tables/q3_indicator_summary.csv` | 16 × 9 矩阵 |
| §5.3 论文节 | `paper/paper.md` §5.3 | ~150-200 行 |
| q3_method.md | `issue/q3_method.md` | ~300 行 |
| q3_data_prep.py | `src/q3_data_prep.py` | ~150 行 |
| q3_association.py | `src/q3_association.py` | ~80 行 |
| q3_milp.py | `src/q3_milp.py` | ~250 行 |

---

## 3. Q4 方法架构

> Q4 = Q3 baseline + 不确定性扰动 + 期望值估计 + 区间

### 3.1 设计原则

1. **不是凭空随机**：6 个扰动因子必须从 2025 实际数据估计分布参数（不能用任意 Beta 分布凑数）
2. **期望 + 区间**：每个 (d, k) 输出 4 个指标的期望值（result4.xlsx 4 列）+ 95% CI（内部 csv）
3. **预算与 Q3 同等约束**：≤ 2025 **同期**（**BIAS 修订** = 2025-09-11~17 7 天，**预算上限 = 23,488.02 元**，详见 §3.4.1）
4. **方法显式可解释**：Two-Stage SP（不是 DRO/DRL）
5. **（BIAS 新增）同期语义严格化**："2025 年同期"=**2025-09-11~17 这 7 天**（不是 2025 任意 7 天，不是全年）

### 3.2 不确定性参数化（**PoC 修订**，不许拍脑袋）

**6 个扰动因子与历史数据来源（**PoC 实测**：必须用 Sheet1 推广单元级 30 天 CV，不是 Sheet3 跨词 CV**）：

| 扰动因子 | 来源 | 估计方法（**PoC 实测**）| 分布假设 | PoC 备注 |
|---|---|---|---|---|
| 竞价（CPC）| Sheet1 推广单元级 消费额/点击量 | 30 天滚动窗口 CV | 对数正态 LogNormal(μ, σ)| PoC: 跨词 CPC CV = 1.92 不可用；用推广单元级 CV 中位数 0.65 代理 |
| 展现量 | Sheet1 推广单元日级 展现量 | 30 天滚动窗口 CV | 对数正态 | PoC: 12 单元 CV 中位数 0.49 |
| 展现位 | Sheet1 推广单元日级 上方位展现量 | 同上 | 截断正态（在 [1, 10]）| PoC: 上方位展现量 sum=6.12M / 推广单元 |
| 点击量 | Sheet1 推广单元日级 点击量 | 同上 | 对数正态 | PoC: 12 单元 CV 中位数 0.61 |
| 浏览量 | Sheet1 推广单元日级 消费额（**代理**：Sheet3 浏览量无日级）| 30 天滚动 CV on Sheet1 消费额 | 对数正态 | ⚠️ 浏览量本身 Sheet3 无日级；用消费额 CV 代理 |
| 注册量 | Sheet2 日期级 新注册数（**代理**：Sheet2 是日级但只到日）| 30 天滚动 CV on Sheet2 | Poisson-Pareto 混合 | ⚠️ 注册数无关键词级 CV，用日级 CV 中位数 |

> **PoC 关键发现**：
> 1. Sheet3 是全年累计 → "跨词 CPC CV = 1.92" 反映"不同关键词之间的差异"，**不能反映"同一关键词每天的不确定性"**
> 2. Sheet1 推广单元日级 30 天滚动 CV（真实跨日变异）：消费 0.46~1.91（中位 0.65）/ 点击 0.45~1.83（中位 0.61）/ 展现 0.33~1.70（中位 0.49）
> 3. 浏览量/注册量 因无关键词-日级 CV，统一用"推广单元级消费额 CV"作为代理

> **工程实现（PoC 修订）**：
> - 数据：Sheet1 推广单元-日 365 天序列（每单元 1 条序列，长度 365）
> - 滑动 CV = std(x[t-30+1:t]) / mean(x[t-30+1:t])
> - 关键词级 CV 用"所在推广单元"的 CV 代理（理由：Sheet3 无日级）
> - 6 因子分布参数统一存 `results/tables/q4_uncertainty_params.csv`

输出：
- `results/tables/q4_uncertainty_params.csv`（每推广单元 × 每关键词 × 6 因子，**Sheet1 推广单元级 30 天 CV**）
- `results/figures/q4_uncertainty_distribution.png`（6 张子图：6 因子的经验分布）

### 3.2.1 关键词级 CV 代理规则（**PoC 新增**）

```python
# 每个关键词 k 在推广单元 p 下，6 因子扰动 σ：
# 公式：kw_cv[k, factor] = unit_cv[p, factor]  (直接用单元 CV)
#
# 理由：Sheet3 无日级数据，无法算同一关键词每天的真实 CV
# 论文 §5.4.1 必须显式说明此代理假设 + 敏感性分析（将 kw_cv 缩放 ±30%）
```

### 3.3 优化：两阶段随机规划 Two-Stage SP（**不是 DRO**）

**为什么选 Two-Stage SP**：
- DRO（distributionally robust optimization）解释成本太高，评委不爱看公式
- 简单 SAA（sample average approximation）太天真，6 因子联合扰动下不可行
- Two-Stage SP 是工业界经典："第一阶段策略 + 第二阶段补足"

**决策变量**：
- 第一阶段（here-and-now）：`y[d,p,k]` ∈ {0, 1} 二元（投不投），同 Q3
- 第二阶段（wait-and-see）：`z[d,p,k,ξ]` ∈ ℝ+ 在场景 ξ 下补足金额

**目标函数**：最小化最坏期望成本（不超预算的前提下最大化期望收益；或等价的最大化期望收益）
```
max  E_ξ [收益(d,p,k, ξ)] - λ · Var_ξ [收益]
```
其中 λ 是风险厌恶系数（推荐 λ=0.5，给出 3 个对比：λ=0/0.5/1.0）。

**约束**：
1. 第一阶段：x[d,p,k], y[d,p,k] 同 Q3
2. 第二阶段：scenario-s 收益预测 ≤ 投入预算 + scenario-s 补足（不允许超）
3. 期望总预算 ≤ 2025 同期（不是每个 scenario 都强约束）

**求解**：Monte Carlo 采样 N=1000 个场景，SAA 近似 + 还原到 MILP。
- 起步：每个场景独立解 Q3 的 MILP，再加权聚合
- 进阶：用 SDDP（stochastic dual dynamic programming）—— **可选，仅当 SAA 时间超过 30 分钟才用**

### 3.4 期望 + CI 输出（**BIAS 修订**：D-Q3Q4-001 已自动确定）

**BIAS 关键发现**：附件 2 `result4.xlsx` 与 `result3.xlsx` **模板列结构 100% 相同**（都是 9 列：日期 / 方案ID / 推广单元 / 关键词 / 投入金额 / 预期展位 / 预期点击量 / 预期浏览量 / 预期注册量）。

→ **D-Q3Q4-001 已自动确定**：严格按 result4 模板 = **4 个预期列**（投入金额 + 预期展位 + 预期点击量 + 预期浏览量 + 预期注册量），**不扩展至 11 列**。
→ 题面 Q4 文字"6 个期望值"是**文字描述**（竞价/展现/展现位/点击/浏览/注册），与模板冲突。**模板是权威约束**（mdc 红线第 1 条）。

**6 期望值的处理路径**：
- `result4.xlsx` 仅填 **4 列**（按模板）
- **6 因子扰动**仅用于内部不确定性建模 + paper §5.4 文字回答 6 个期望值

输出每个 (d, k) 的：
- **result4.xlsx 写入**：投入金额 + 4 个预期值（展位/点击/浏览/注册）
- **内部 csv**：6 个期望值（sample mean over 1000 scenarios）+ 6 个 95% CI + 6 个标准差
- **paper §5.4 文字**：必须显式给出 6 期望值（即使 Excel 只有 4 列）

**Q4 6 因子扰动逻辑（**BIAS 修订**）**：
- 6 因子：竞价 / 展现量 / 展现位 / 点击量 / 浏览量 / 注册量
- 每个 (d, k) 组合的预测 = base_value × (1 + ε_i)，其中 ε_i ~ N(0, CV_i²)，CV_i 来自 Sheet1 推广单元级 30 天滚动
- base_value 来自 §2.2.1 代理变量法 + §2.4 Layer 4 比值

### 3.4.1 Q4 同期预算精确值（**BIAS 新增**）

> **关键预算上限**：Q4 同期 = **2025-09-11~17 7 天**（**不是 2025 全年 / 任意 7 天**）。

**PoC 实测预算上限**：

| 项 | 值 |
|---|---|
| 2025-09-11~17 总消费 | **23,488.02 元** |
| 日均 | 3,355.43 元 |
| 推广单元-日均 max | 2,721.19 元 |
| 推广单元-日均 min | 0.00 元（部分单元 9 月无投放）|
| 推广单元-日均 中位 | 20.86 元 |
| 推广单元-日 记录数 | 77（不是 7×12=84，有单元未投放）|

→ **Q4 约束**：`∑_{d ∈ 2026-09-11~17} y[d, k] ≤ 23,488.02 元`（**注意**：与 Q3 的 51,164.93 元不同，因时段不同）

输出：
- `results/excel/result4.xlsx`（4 列，按 result3 模板）
- `results/tables/q4_expected_with_ci.csv`（**6 期望 + 6 CI** × 7 天 × 推广单元 × 关键词）
- `results/figures/q4_fan_chart.png`（展示每个指标 7 天的 fan chart，含期望 + 95% 区间）

### 3.5 校验机制（4 项）

| 校验 | 做法 | 通过标准 |
|---|---|---|
| **分布校准** | 用 KS 检验验证 6 因子经验分布拟合（**PoC：每个推广单元单独 KS**）| p > 0.05（不能 reject）|
| **期望守门** | 期望总投入 ≤ 2025 同期 | 严格 ≤（**PoC：2025-09-11~17 同期预算需先抽取**）|
| **鲁棒性** | 改 λ ∈ {0, 0.5, 1.0} 看投入策略是否稳定 | Top 50% 词集合 overlap ≥ 80% |
| **不确定性传播** | 6 指标 CV 加权 vs 总 CV | 总收益 CV ∈ [0.10, 0.50]（不过分散）|

### 3.6 输出物清单（Q4 实施 agent 交付）

| 产出 | 路径 |
|---|---|
| result4.xlsx | `results/excel/result4.xlsx` |
| 不确定性参数 | `results/tables/q4_uncertainty_params.csv` |
| 不确定性分布图 | `results/figures/q4_uncertainty_distribution.png` |
| 期望+CI 长表 | `results/tables/q4_expected_with_ci.csv` |
| Fan chart | `results/figures/q4_fan_chart.png` |
| §5.4 论文节 | `paper/paper.md` §5.4 |
| q4_method.md | `issue/q4_method.md` |
| q4_data_prep.py | `src/q4_data_prep.py` |
| q4_uncertainty.py | `src/q4_uncertainty.py` |
| q4_solver.py | `src/q4_solver.py` |

---

## 4. 国奖加分点（**4 项，不堆方法，只强化业务价值 + 模型可解释**）

| 加分点 | 在哪一章节 | 为什么是国奖 |
|---|---|---|
| **关键词关联网络图**（不只是统计分布）| Q3 §5.3.3 | 业务可解释（SEM 投放经理能直接读图决策）+ 题目"关联关系"语义落实 |
| **MILP 目标函数 + 约束的 LaTeX 公式** | Q3 §5.3.2 | 评委看公式比看代码重要 + 模型严谨 |
| **6 因子不确定性来源显式标注**（每张图标注"参数来自 Sheet1/Sheet3 历史 30 天 CV"）| Q4 §5.4.1 | 不是黑盒随机，是基于实际数据 |
| **鲁棒性 3 档 λ 对比图**（λ=0 vs 0.5 vs 1.0）| Q4 §5.4.4 | 评委喜欢看"参数敏感性 + 鲁棒性结论" |

---

## 5. 给执行 agent 的红线（**继承 mdc v3 + Q2 规划 + PoC 验证**）

1. ❌ **禁止自创输出格式**——result3/4 列名 = 9 列**已严格对齐附件 2 模板**（**BIAS 修订**：result3 与 result4 模板完全相同，D-Q3Q4-001 已自动确定 = 4 列）
2. ❌ **禁止扩展时间范围**——result3 = 16 天 / result4 = 7 天
3. ❌ **禁止改变决策粒度**——必须 = 推广单元 × 关键词 × 天
4. ❌ **禁止引入未在本文档的方法**（Transformer/Attention/DRL/GAN/NSGA-II/CPLEX）
5. ❌ **禁止把 4 指标简化为 1 指标**——4 列必须都写
6. ❌ **禁止使用商业求解器商业授权**——PuLP/OR-Tools 是首选
7. ❌ **禁止自创随机分布假设**——必须从 Sheet1 推广单元级 30 天 CV 派生（**PoC 修订**，不是 Sheet3 跨词 CV）
8. ❌ **禁止修改 Q1 / Q2 已确定的口径**——评分函数 / 权重 / 假日扣分 / 5 类规则 / 阈值
9. ❌ **禁止跳过读附件 2 模板**——每次跑前先 re-read 一次
10. ❌ **禁止"做了一堆动作但忘了写 RUN_LOG"**——每动作 1 行
11. ❌ **（PoC 新增）禁止绕过 §2.2.1 代理变量法直接做关键词日级预测**——Sheet3 无日级数据，必须经代理公式
12. ❌ **（PoC 新增）禁止用"统一缩放因子"代替单元-日预算差异**——单元差异 1400×，必须按单元设上限

**允许的方法栈白名单**：
- ✅ FP-Growth（关联规则挖掘）
- ✅ MILP via PuLP / OR-Tools
- ✅ Two-Stage SP via SAA / SDDP（可选）
- ✅ CRITIC + Bootstrap + 龙卷风（从 Q1 复用）
- ✅ 对数正态 / 截断正态 / 泊松拟合
- ✅（PoC 新增）代理变量法（关键词级指标 = 推广单元-日级 × 消费占比）

---

## 6. 执行顺序（S1-S9，继承 Q2 规划风格）

| 阶段 | 内容 | 时间预算 |
|---|---|---|
| **S1** | 备份当前 + WORK_STATE/DECISION_LOG 同步 Q3/Q4 任务 | 10 min |
| **S2** | 读附件 2 模板 + Sheet1/2/3 + 5 维特征 pkl 准备 | 30 min |
| **S3** | 写 `src/q3_data_prep.py` + 5 维特征 pkl | 45 min |
| **S4** | 写 `src/q3_association.py` + 关联网络图 + 规则 csv | 60 min |
| **S5** | 写 `src/q3_milp.py`（MILP 公式 + PuLP 实现）| 90 min |
| **S6** | 跑通 result3.xlsx + 4 校验 | 45 min |
| **S7** | 写 `issue/q3_method.md` + `paper/paper.md` §5.3 | 90 min |
| **S8** | Q4 启动：不确定性参数化 + Two-Stage SP | 90 min |
| **S9** | §5.4 + 所有校验 + DoD | 60 min |
| **总计** | | **9 小时 ≈ 1 上午** |

---

## 7. DoD（成功标准）

### Q3 完成判定
- [ ] result3.xlsx 行数 ∈ [5K, 20K]，9 列名严格对齐
- [ ] 预算 ≤ 2025 同期（不留 buffer）
- [ ] 入选项 100% 来自黄金+重点+潜力
- [ ] 关联网络图输出 + 关联规则 csv ≥ 50 条
- [ ] MILP 求解时间 < 10 分钟
- [ ] 4 个指标历史回放比率 ∈ [0.85, 1.15]
- [ ] §5.3 论文节有 LaTeX 公式 ≥ 5 行
- [ ] q3_method.md 评审可读

### Q4 完成判定
- [ ] result4.xlsx 行数 ∈ [1K, 5K]，6 期望列齐全
- [ ] 6 因子 KS 检验 p > 0.05
- [ ] 期望总预算 ≤ 2025 同期
- [ ] λ ∈ {0, 0.5, 1.0} Top 词集合 overlap ≥ 80%
- [ ] §5.4 论文节含不确定性来源标注 + 鲁棒性对比
- [ ] q4_method.md 评审可读

### 通用（继承 Q2）
- [ ] RUN_LOG 30+ 行
- [ ] WORK_STATE Q3/Q4 路线图状态 ✅
- [ ] DECISION_LOG D-012/D-013 决策追加

---

## 8. 与 Q1 / Q2 衔接 / 不要做的修改

- **不能改**：Q1 综合分 50.7、Q1 4 维度权重、Q1 节日扣分
- **不能改**：Q2 5 类规则（黄金/重点/潜力/问题/无效）
- **不能改**：Q2 阈值定义（中位数分桶）
- **能改**：result3/4 产出物命名（除 schema 外不影响 Q1/Q2）
- **能改**：MILP 目标函数权重 α/β/γ（但需在 §5.3 显式说明）
- **能改**：λ 风险厌恶系数（要在 §5.4 显示 3 档对比）

---

## 9. 引用

| 引用 | 用途 |
|---|---|
| `issue/q2_implementation_plan.md` | Q2 5 项锁定决策 + D-Q2-001~005 |
| `issue/q1_fix_plan.md` | P0-0 节假日修正 + Q1 评分链路 |
| **`PoC_REPORT.md`** | **PoC 数据探测结论（2026-09-12 16:42，影响 §2.2.1/§2.3/§3.2）** |
| `src/utils.py` | 加载 Sheet / 数据读 |
| `src/plot_style.py` | 统一绘图 |
| `data/processed/q2/keyword_classified.pkl` | Q3 必备输入 |
| `results/tables/q2_thresholds.json` | Q3 可选参考（不强引用）|
| `tools/poc_q3q4_data_probe.py` | PoC 数据探测脚本（可重跑）|

---

## 10. PoC 修订变更日志（与原版 14:50 对比）

| 章节 | 原版问题 | PoC 修订 |
|---|---|---|
| §1.1 | Sheet3 未注明时间口径 | **标注"全年累计，非日级"**；列尾空格提示加粗 |
| §1.4 | 不存在 | **新增**：时间粒度标准化表 + 16 天预算详情（51,164.93 元）|
| §2.2 Layer 1 | 5 维特征未注口径 | 加注"Sheet3 全年累计" |
| §2.2.1 | 不存在 | **新增**：代理变量法（5 个公式 + 2 个关键假设 + 注册两步分配链路）|
| §2.3 目标函数 | `max Σ benefit/y` 数学漏洞（y→0 发散）| 改为线性 LP `max Σ (α·r_click + β·r_browse + γ·r_reg) · y` |
| §2.3 约束 2 | 用"统一缩放因子 1.0"代替单元预算 | 改为"按推广单元历史日均设上限 B_unit[p,d]"（PoC 实测 max=2237 / min=1.58）|
| §2.3 约束 4 | 关联规则 conf>0.8 硬约束（易 infeasible）| 改为目标函数软奖励 `+ δ · 应用率`；附 3 级 infeasible fallback |
| §2.3 总预算 | 未给具体数值 | 写死 **51,164.93 元**（PoC 实测 16 天总消费）|
| §2.4 Layer 4 | 4 指标公式抽象，未引用代理变量法 | 改为"4 个历史比值 × y"线性形式，明确引用 §2.2.1 |
| §3.2 | CPC CV 用 Sheet3 跨词（不可用）| **明确**：用 Sheet1 推广单元日级 30 天 CV；PoC 实证 12 单元 CV 中位 0.45~0.65 |
| §3.2.1 | 不存在 | **新增**：关键词级 CV 用所在推广单元 CV 代理 + 敏感性规则 |
| §5 红线 | 11 条 | 新增 2 条 PoC 关键约束（红线 11/12）|

---

## 11. BIAS 修订变更日志（2026-09-12 16:57 · 与 16:42 PoC 修订对比）

| 偏差 ID | 章节 | 原版问题 | BIAS 修订 |
|---|---|---|---|
| **P0-1** | §2 / §2.4 | Q3 被误标"预测"，但 16 天 = 2025 历史 | 新增 §2.0 "Q3 = 历史反事实优化"；Layer 4 改名"反事实估算" |
| **P0-2** | §3.4 / §3.5 | Q4 同期预算未抽取具体值 | 新增 §3.4.1 "Q4 预算上限 = 23,488.02 元"（2025-09-11~17 实测）|
| **P0-3** | §3.4 / §5-1 | "D-Q3Q4-001 待决策"4/6 列 | **已自动确定**：result4 与 result3 模板相同 = 4 列；论文 §5.4 文字给 6 期望 |
| **P1-1** | §1.2 列 6 | "预期展位"口径仅一句话 | 新增 §1.2.1 口径选择表（上方位展现量 = 27% 占比）|
| **P1-2** | §2.3 Layer 3 | 决策变量 `(d, p, k)` 三维冗余 | 简化为 `(d, k)`（k 已唯一确定 (方案, 推广单元)）；决策变量从 174,528 减至 14,544 |
| **P1-3** | §1.4 / §2.3 约束 3 | 16 天预算合并 51,164.93 | 分段：02 月 13,681.15 + 08 月 37,483.78 |
| **P2-1** | §3.4 | Q4 paper 6 期望要求不明确 | §3.4 明确"paper §5.4 文字给 6 期望" |
| **P2-2** | §2.1 | "投放模型"语义不清 | 新增设计原则 5：投放模型 = MILP 公式 + 业务规则 + 关联规则（三层）|
| **P2-3** | §3.1 | Q4 "同期"语义未严格 | 新增设计原则 5：同期 = 2025-09-11~17 7 天 |
| **P2-4** | §1.1 | result2 列名缺"词"字 | 加"词"：黄金词/重点词/潜力词/问题词/无效词 |

---

最后更新：2026-09-12 16:57（含 PoC + BIAS 双轮修订）· 用于 Q3/Q4 实施交接 · 严禁偏离红线 §5
