# Q2 方法说明 · 关键词 5 类分类

## 1. 目的
对附件 1 中 **2227** 个关键词（含 **1337** 个有效）做 5 分类，给出差异化运营建议。

## 2. 方法

### 2.1 特征工程（6 维）
- `log消费额` = log1p(消费额)
- `CPC` = 消费额 / 点击量（直接用）
- `log展现量` = log1p(展现量)
- `点击率` = 点击量 / 展现量
- `注册转化率` = 注册数 / 点击量（NaN → 0）
- `跳出率`（NaN → 中位数填充）

### 2.2 GMM + BIC 选 K
- 算法：Gaussian Mixture Model（高斯混合）
- 协方差类型：full（最一般）
- 候选 K：2 ~ 8
- 选 K 准则：BIC 最小（Bayesian Information Criterion）
- 实际最佳 K = **8**（BIC = -77092.6）

### 2.3 业务命名（启发式规则）
基于聚类中心在 6 维特征空间的归一化排名，按业务启发式匹配：

| 业务名 | 启发式 |
|--------|--------|
| 高价值转化型 | CPC 低 + 注册率高 |
| 稳定拓展型 | 展现高 + CPC 合理 |
| 潜力挖掘型 | 展现高 + 注册低 + CPC 低（待优化） |
| 高成本浪费型 | CPC 高 + 消费高 + 注册低（应暂停） |
| 低效长尾型 | 其他（兜底） |

## 3. 聚类结果（K=8）

| 类别 | 名称 | 关键词数 | 占比 | 平均消费 | 平均 CPC | 平均注册率 |
|------|------|---------|------|---------|---------|-----------|
| 0 | 高价值转化型 | 145 | 6.5% | 9052 | 4.72 | 0.0000 |
| 1 | 稳定拓展型 | 263 | 11.8% | 41 | 1.30 | 0.0000 |
| 2 | 低效长尾型 | 888 | 39.9% | 0 | nan | 0.0000 |
| 3 | 高成本浪费型 | 1 | 0.0% | 2117 | 88.19 | 0.0000 |
| 4 | 稳定拓展型二 | 51 | 2.3% | 239 | 0.70 | 0.0000 |
| 5 | 低效长尾型二 | 122 | 5.5% | 2 | 1.15 | 0.0000 |
| 6 | 稳定拓展型三 | 320 | 14.4% | 269 | 1.09 | 0.0000 |
| 7 | 低效长尾型三 | 437 | 19.6% | 5 | 1.02 | 0.0000 |

## 4. 论文可写要点

### 4.1 业务解读
- **高价值转化型**：核心词，应加大预算
- **稳定拓展型**：补量词，可稳步扩
- **潜力挖掘型**：优化词，优化创意/落地页后可释放
- **高成本浪费型**：应暂停
- **低效长尾型**：长期观察

### 4.2 国一差异化
- 不止"4 象限 / 5 分类"硬阈值
- GMM 自适应选 K（不必拍脑袋选 5）
- 业务命名让算法贴近业务

## 5. 输出文件
- `results/tables/q2_keyword_classification.csv`（每关键词 + 类别）
- `results/tables/q2_cluster_summary.csv`（聚类中心 + 业务汇总）
- `results/figures/q2_gmm_clusters.png`（PCA 散点 + BIC 曲线）
- `data/raw/attachments/result2.xlsx`（题目要求位置）
- `issue/q2_method.md`（本文档）

## 6. 可复现性
- 随机种子：42
- n_init=3（多次随机初始化取最优）
- 数据：`data/processed/q1/keyword_total.pkl`（来自 `q1_data_prep.py`）
