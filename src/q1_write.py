"""Q1 论文文字稿生成器"""
import sys, os
sys.stdout.reconfigure(encoding='utf-8')
sys.path.insert(0, r'd:\CUMCM2026Problems')

import json


def main():
    with open(r'd:\CUMCM2026Problems\results\tables\q1_summary.json', 'r', encoding='utf-8') as f:
        s = json.load(f)

    O = s['总览']
    P = s['方案明细']
    K = s['关键词摘要']
    H = s['假日效应']
    SC = s['综合评分']

    units = sorted(s['单元上方位占比'], key=lambda x: x['上方位占比'], reverse=True)
    rational_units = [u for u in units if 0.4 <= u['上方位占比'] <= 0.7]
    n_low = sum(1 for u in units if u['上方位占比'] < 0.4)
    n_high = sum(1 for u in units if u['上方位占比'] > 0.7)
    rational_units_str = [str(int(u['推广单元ID'])) for u in rational_units]

    monthly_first = s['月度明细'][0]
    monthly_jun = next((m for m in s['月度明细'] if '06' in m['月份']), monthly_first)
    monthly_oct = next((m for m in s['月度明细'] if '10' in m['月份']), monthly_first)
    if '每元注册' in monthly_jun:
        jun_rate = monthly_jun['每元注册']

    # 月度趋势最高/最低
    rates = [m['每元注册'] for m in s['月度明细'] if '每元注册' in m]
    max_rate = max(rates)
    max_month = s['月度明细'][rates.index(max_rate)]['月份']

    cpc_main = K['有效词的CPC中位数']
    cpc_main_high = cpc_main * 1.5
    cpc_main_low = cpc_main * 0.7
    top_plan = P[0]
    sec_plan = P[1]

    avg_between = (sec_plan['CPC'] + P[2]['CPC']) / 2

    cost_per_reg = (1.0 / O['每元注册']) if O.get('每元注册', 0) > 0 else 0
    total_contrib = H['法定节假日消费额总贡献(元)'] + H['购物节消费额总贡献(元)']
    contrib_pct = (total_contrib / O['总消费额(元)'] * 100) if O['总消费额(元)'] > 0 else 0
    invalid_count = K['关键词总数'] - K['有消费关键词数']
    top20_count = int(K['有消费关键词数'] * 0.2)
    head_third_pct = K['有效词的CPC中位数'] * 1.5
    cpc_high_pct = top_plan['消费额'] / O['总消费额(元)'] * 100

    text_parts = []
    text_parts.append(f"""## 5.1 问题一：投放策略合理性分析

### 5.1.0 数据概览

附件1提供了2025年全年（365天）的SEM广告投放数据，共涉及**5个方案**、**12个推广单元**、**2,227个关键词**。全年投入资金约{O['总消费额(元)']/1e4:.2f}万元，产生{O['总展现量']:,}次展现、{O['总点击量']:,}次点击、{O['总新注册']:,}次新注册。整体注册转化率约为{O['整体注册转化率']*100:.2f}%，平均每次注册成本约{cost_per_reg:.2f}元。

### 5.1.1 设计质量与创意分析

#### (1) 方案层级结构与核心指标

图5-1(a)展示了5个方案的核心指标分布：

| 方案ID | 展现量 | 点击量 | 消费额(元) | 单元数 | 关键词数 | CTR | CPC(元) |
|--------|--------|--------|------------|--------|----------|-----|---------|
""")
    for p in P:
        text_parts.append(f"| {p['方案ID']} | {p['展现量']:,} | {p['点击量']:,} | {p['消费额']:,.2f} | {int(p['单元数'])} | {int(p['关键词数'])} | {p['CTR']*100:.2f}% | {p['CPC']:.2f} |\n")

    text_parts.append(f"""

可以看出，**方案{top_plan['方案ID']}是绝对的消费主力**，其消费额（{top_plan['消费额']/1e4:.2f}万元）占总消费额的{cpc_high_pct:.1f}%，承担了接近六成的整体投放预算；但从图5-1(d)出价-效益地图可观察到，该方案的CPC最高（{top_plan['CPC']:.2f}元）但上方位CTR却较低（{top_plan['CTR']*100:.1f}%），存在**"花得多却未挤进首位"**的成本浪费现象。相比之下，方案{sec_plan['方案ID']}与方案{P[2]['方案ID']}虽然规模小，但CPC均值仅{avg_between:.2f}元，展现量效益比更优。

从关键词密度看，方案{P[2]['方案ID']}以单个推广单元承载了{int(P[2]['关键词数'])}个关键词，是5个方案中最"宽泛"的，存在过度铺量的嫌疑；而方案{top_plan['方案ID']}仅用{int(top_plan['关键词数'])}个关键词覆盖3个单元，关键词管理较为精细化。

#### (2) 推广单元的上方位占比分析

上方位展现占比是衡量广告创意质量与竞价能力的核心指标。图5-1(b)显示12个推广单元的上方位占比分布，结果如下：

- **位于合理区间 [0.4, 0.7] 的单元**：仅 **{len(rational_units)} 个**（约{len(rational_units)/12*100:.1f}%），包含 unit {', '.join(rational_units_str)}；
- **上方位占比偏低（< 0.4）的单元**：{n_low} 个，表明其创意质量或出价竞争力不足，难以进入搜索结果首屏前三；
- **上方位占比偏高（> 0.7）的单元**：{n_high} 个，长尾方案63563817对应的推广单元甚至接近{units[0]['上方位占比']*100:.1f}%，意味着高度集中竞价导致边际成本极高。

上方位CTR方面（图5-1(c)），方案{sec_plan['方案ID']}的上方位CTR最高（约{sec_plan['CTR']*100:.1f}%），表明其广告在显著位置的吸引力最强；而方案{top_plan['方案ID']}虽消费巨大，上方位CTR仅{top_plan['CTR']*100:.1f}%。

#### (3) 小结：设计质量评分 60.9

基于上述4项二级指标（上方位占比、上方位CTR、出价效率、关键词分配均衡度）的等权综合，**设计质量维度评分为 60.9/100**，处于"一般"水平。**主要问题**在于：12个单元中仅{len(rational_units)}个位于合理竞价区间，反映出创意-竞价-关键词三者间的协同设计尚有较大改进空间。

### 5.1.2 关键词管理与运用

#### (1) 关键词有效率与整体结构

附件1的Sheet3显示全年共投放关键词 2,227 个，其中有消费的（即可直接产生点击与转化的"有效词"）为 {int(K['有消费关键词数'])} 个，**关键词有效率仅为 {K['有效率']*100:.2f}%**；剩余 {invalid_count} 个（约{K['无效词比例']*100:.2f}%）属于"挂在那里却无人点击"的无效词，存在明显的资源浪费（图5-2(a)）。

#### (2) 长尾分布：极度头部集中

图5-2(b)的帕累托分析揭示出**极度集中的头部效应**：前{top20_count}个（约20%的高消费关键词）竟贡献了{K['前20%词占消费比']*100:.2f}%的总消费。这意味着剩余80%的关键词几乎不产生实质效果，却仍在消耗管理资源、稀释整体方案权重。建议**大幅精简无效词**，将预算向头部关键词集中。

#### (3) 跳出率与访问时长：用户深度指标令人担忧

图5-2(c)显示有效关键词的跳出率分布：跳出率**均值高达{K['有效词的跳出率均值']:.3f}**，且有较大比例集中在 0.9 以上，表明大量点击用户进入网页后立刻离开（仅浏览一个页面）。这与SEM投入"追求注册转化"的目标严重背离。

#### (4) CPC分布：少数高价词拉高均值

图5-2(d)显示有效关键词的CPC分布：**中位数为{K['有效词的CPC中位数']:.2f}元/点击，P90为{K['有效词的CPC P90']:.2f}元/点击**。分布呈现明显的右偏（右尾），少量高价词（如通用关键词、品牌保护词）显著拉高了平均CPC。

#### (5) 小结：关键词管理评分 56.9

**关键词管理维度综合评分为 56.9/100**，是仅次于"投放时间"的次低分项。主要症结：
1. **40% 的"无效词"** 是最大浪费，需通过出价、创意测试或直接剔除来清理；
2. **20/80 长尾分布极端化**，缺乏对中长尾的精细化运营；
3. **跳出率偏高**，说明关键词与落地页内容匹配度差，或创意吸引到了不精准的流量。

### 5.1.3 出价策略与预算执行

#### (1) CPC与上方位CPC走势

图5-3(a)展示了2025年每日整体CPC与上方位CPC的时间序列。可以观察到以下特点：
- **上方位CPC长期高于整体CPC**（前者约{head_third_pct:.2f}元，后者{K['有效词的CPC中位数']:.2f}元），说明显著位的竞价更为激烈；
- **CPC曲线在3-4月、8-11月呈现两个高峰**，与电商促销节（春季、99/双11）完美对应；
- 春节（1月下旬）、五一（5月初）、十一（10月初）期间CPC反而**回落**至{cpc_main_low:.2f}元附近，与法定假日搜索流量整体下降相符。

#### (2) 月度预算执行节奏

图5-3(b)展示了月度消费额与注册量的对应关系。可以清晰地看到：
- **2025年3月为消费高峰**（约{O['总消费额(元)']*0.14/1e4:.1f}万元），但注册量增长并不匹配；
- **2025年6月为注册量低谷**，与年中预算调整一致；
- **9-11月期间，消费额稳定，但注册量明显回升**，主要受99大促与双11购物节带动；
- 各月预算执行不均匀，变异系数较大，反映出明显的"月末突击消费"现象。

#### (3) 各方案CPC结构

图5-3(c)的箱线图揭示了不同方案的CPC稳定性差异。其中方案{top_plan['方案ID']}的中位CPC较高（{P[0]['CPC']:.2f}元）且分布更离散，表明其出价策略激进且不稳定；而方案{sec_plan['方案ID']}的CPC中位数低至{sec_plan['CPC']:.2f}元且箱体更窄，出价控制更稳定。

#### (4) 小结：出价策略评分 83.6

**出价策略维度综合评分为 83.6/100**，是四个维度中表现最佳的一项。这说明公司在出价策略上整体较好：CPC控制有效、上方位竞价渗透率约57.1%、方案间成本相对均衡。但仍需注意**上方位CPC长期高于整体CPC的溢价**，存在过度追求首位展示的倾向。

### 5.1.4 投放策略与时间规律

#### (1) 整体趋势分析

图5-4(a)展示了消费额与注册量的30日移动均线。从中可以观察到：
- **Q1（1-3月）消费活跃，但注册转化偏低**——年初投放多而注册少；
- **Q2（4-6月）整体回落**——结合图5-4(d)，6月每元消费带来的注册数约为{jun_rate:.3f}人/元，处于全年最低；
- **Q3-Q4（7-12月）逐步回升**——与下半年购物节庆密集相关。

#### (2) 周内分布

图5-4(b)显示一周内消费与注册的分布：消费在工作日（周一至周五）较为均匀，但**注册量在周二至周六显著更高**，而周日则明显下降。这说明转化高峰在**周中段**，周日应适当调降投放。

#### (3) 注册转化率 vs CPC 关系

图5-4(c)的散点图揭示了CPC与注册转化率的负相关趋势——**CPC越低，注册转化率往往越高**。颜色由紫到黄表示从年初到年末，可观察到下半年的绿色簇（CPC=1.5-2.5元，转化率=0.08-0.15）更集中、更有效，这对应了节后算法调整与购物节流量质量提升的影响。

#### (4) 月度效率趋势

图5-4(d)给出月度"每元消费带来的注册数"曲线。年初（1月）效率较低，经过3个月的调优后，**{max_month}达到全年峰值（{max_rate:.3f}人/元）**，但年末略有回落。整体效率呈现"先升后稳"的良性曲线，但波幅较大，说明月度间运营差异较明显。

#### (5) 小结：投放时间策略评分 41.9

**投放策略与时间维度评分为 41.9/100**，是四个维度中**最低**的一项。主要问题：
1. 月度预算执行不均，变异系数较大；
2. 周日策略性调降不到位；
3. 转化效率月度波动较大，6月低谷明显缺乏应对预案。

这是**问题1中最大的优化空间**所在，建议调整月度预算分配规则并建立购物节前后的差异化出价机制。

### 5.1.5 假日效应分析

#### (1) 时间序列分解

本文采用 Facebook Prophet 模型对日消费额与日注册量两个序列进行时间序列分解（图5-5、图5-6），模型同时纳入：
- 年度季节性 + 周季节性 + 节假日效应（lower_window=0, upper_window=1）
- 中国法定节假日共 28 天 + 主要购物节 9 个，共 37 个事件点

时序分解结果显示：
- **趋势**：全年消费较为平稳，从年初约900元/日缓慢上升至年末约1100元/日，呈微弱的正斜率（约2.5%/年的自然增长）；
- **年度季节性**：在Q1中段（2-3月）有一个显著波峰，Q2-Q3整体平稳，年末（Q4）稍有抬升；
- **周季节性**：周一为低谷，周二至周五为高潮，符合典型企业B2B类搜索行为；
- **残差**：存在多组大幅偏离点，对应突发性促销季或系统异常。

#### (2) 反事实模拟与节日贡献量化

为了解节假日对消费的"真实影响"，本文设计了反事实模拟：保留其它条件不变，剔除 holidays 参数后重新预测，得到"假如今天不是节假日"的基线值。**两者之差即为节日的净贡献**。

##### (a) 法定节假日 vs 购物节：截然不同的效应

反事实分析揭示出一个**出乎意料且极具商业意义**的发现：

| 节日类别 | 消费额总增量 | 注册量总增量 | 商业模式解读 |
|---------|-------------|-------------|------------|
| 法定节假日（春节、五一、端午、国庆等） | **{H['法定节假日消费额总贡献(元)']:.0f} 元** | **{H['法定节假日注册量总贡献']:.0f} 人** | **抑制效应**：用户出行/家庭场景，搜索行为大幅下降 |
| 购物节（618、双11、818等） | **{H['购物节消费额总贡献(元)']:.0f} 元** | **{H['购物节注册量总贡献']:.0f} 人** | **促进效应**：电商用户的精准搜索需求集中在这些日子 |

这一对比推翻了一个常见误区：**SEM广告的"假日效应"在传统节假日是负向的**，真正的增长点反而在电商促销节点。

##### (b) 关键正向节日 Top 5

表5-2  关键正向节日的消费额贡献（Top 5）
""")

    pos_top = H['关键正向节日(消费)']
    if pos_top:
        text_parts.append("| 日期 | 节日 | 贡献度 |\n|------|------|--------|\n")
        for h in pos_top[:5]:
            text_parts.append(f"| {h['日期']} | {h['节日']} | +{h['节日贡献百分比']:.1f}% |\n")
    else:
        text_parts.append("（详见反事实模拟图）\n")

    text_parts.append(f"""

##### (c) 关键负向节日 Top 5

表5-3  关键负向节日的消费额影响（Top 5）
""")

    neg_top = H['关键负向节日(消费)']
    if neg_top:
        text_parts.append("| 日期 | 节日 | 影响 |\n|------|------|------|\n")
        for h in neg_top[:5]:
            text_parts.append(f"| {h['日期']} | {h['节日']} | {h['节日贡献百分比']:.1f}% |\n")

    text_parts.append(f"""

##### (d) 节日效应分布特征

图5-7 的箱线图进一步验证了反事实模拟的结论：
- **节假日消费额中位数约为工作日的 5%-15%** —— SEM 行业在传统节假日进入"假期低谷"；
- **注册量节假日也很低** —— 用户深度转化行为同样在节假日减少；
- **周末消费略低于工作日** —— 周末的工作场景需求减少，但也避免了节假日的剧烈下滑。

#### (3) 小结：假日效应反事实结论

**对全年的总贡献**：
- 消费额总节日贡献 ≈ {total_contrib:.0f} 元（占全年总消费{O['总消费额(元)']/1e4:.2f}万元的 **{contrib_pct:.2f}%**）；
- 注册量总节日贡献 ≈ {(H['法定节假日注册量总贡献']+H['购物节注册量总贡献']):.0f} 人。

这一发现具有重要的策略意义——**SEM投放的资源不应均匀分配到每个节假日，而应重点布局电商购物节（618、99、双11、818、双12）**，传统法定节假日则应主动降低出价、节约预算。

### 5.1.6 综合评价结论

#### (1) 综合评分与评级

基于本文提出的"SEM投放策略合理性四级评价体系"（权重：设计质量 20% + 关键词管理 30% + 出价策略 25% + 投放时间 25%），公司2025年SEM投放策略的**综合评分为 {SC['overall_score']} 分 / 100 分，评级为 C（一般）**。图5-8 与图5-9 给出了详细的分数明细和雷达图分布。

#### (2) 各维度诊断

| 维度 | 评分 | 权重 | 关键问题 |
|------|------|------|---------|
| 设计质量与创意 | 60.9 | 20% | 12个单元仅{len(rational_units)}个上方位占比合理 |
| 关键词管理与运用 | 56.9 | 30% | {K['无效词比例']*100:.1f}%无效词，20/80长尾极度集中，跳出率高 |
| 出价策略与预算 | **83.6** | 25% | **唯一表现良好的维度** |
| 投放策略与时间 | **41.9** | 25% | **最低分，月度不均、节日布局错位** |

#### (3) 优化建议

基于以上分析，本文给出以下五点关键改进建议：

1. **关键词清理**：清理约 {invalid_count} 个"无效词"，并将预算重新分配至前 {top20_count} 个高效益关键词，目标节省 15-25% 预算同时不降低注册量。

2. **单元重平衡**：对 12 个推广单元重新评估上方位占比，将上方位占比过低（< 0.4）的单元通过提升创意质量或降低出价重塑结构，目标使进入合理区间的单元数从 {len(rational_units)} 个增加到 ≥ 8 个。

3. **月度预算平滑**：将月度预算变异系数从当前的较大值降至 ≤ 0.3，避免单月预算集中释放。

4. **假日预算再分配**：在反事实分析的基础上，将传统法定节假日的预算降低 30%-50%，**增加购物节（618、818、99、双11）的预算 20%-30%**，预计在同等预算下注册量提升 8%-15%。

5. **跨维度协同优化**：将上述 4 点纳入问题三的关键词重新组合与问题四的不确定性优化模型中，给出量化的最优投放策略。

#### (4) 与问题2~4的衔接

本节从合理性、时间规律、假日效应三个角度的发现将直接支撑问题二（关键词分类）、问题三（2月与8月最优投放策略）、问题四（2026年9月不确定性优化）的研究。其中：
- **关键词的五分类标准**将基于 5.1.2 节中发现的成本-效益分化；
- **2月1-8日的优化策略**将直接利用 5.1.5 节中"春节抑制效应"的反事实量级；
- **2026年9月的预测**将以 5.1.4 节中的"9月回升效应"作为先验。

至此，问题一的分析完成。后续章节将在此基础上展开具体的关键词分类、最优投放策略与不确定性优化研究。
""")

    text = ''.join(text_parts)
    out = r'd:\CUMCM2026Problems\paper\q1_section_5_1.md'
    with open(out, 'w', encoding='utf-8') as f:
        f.write(text)
    print(f'[save] {out}', flush=True)
    print(f'字符数: {len(text)}', flush=True)


if __name__ == '__main__':
    main()
