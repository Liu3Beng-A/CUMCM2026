# 问题一 自评 & 改进方案

> **范围**：题目 C 题 "中小企业 SEM 推广效果分析" —— 问题一（建立指标体系并对附件 1 中各方案进行投放效果量化评估）
> **评估人**：AI 助理
> **评估日期**：2026-09-11（更新）
> **状态**：用户已确认权重法 + 区间法 → 其余项待敲定

---

## 一、问题回顾

题目要求：
1. 参考论文模板 6.1 节 "指标体系"，建立指标体系；
2. 计算各指标在附件 1 数据上的取值；
3. 对每个方案进行投放效果量化评估；
4. 给出可视化结果和文字分析。

---

## 二、整体架构

```
[附件1 Sheet1/2/3]
        ↓
[q1_data_prep.py]   ← 多维度聚合（方案 / 单元 / 日 / 关键词）
        ↓
        ├──→ [q1_scoring.py]   4维度12+指标 → 综合评分
        └──→ [q1_prophet.py]   时序分解 + 反事实模拟
                    ↓
        [q1_plots.py]      ← 11 张专业图
        [q1_write.py]      ← 5.1 节论文文字稿
```

四步闭环：**数据 → 指标 → 评分 → 文字稿/图**。

---

## 三、详细方案

### 3.1 评分体系（`q1_scoring.py`）

四个一级维度、十二个二级指标，权重如下：

| 一级维度 | 权重 | 二级指标 | 评估角度 |
|---------|------|---------|---------|
| 设计质量与创意 | 20% | 上方位占比 / 上方位 CTR / 出价效率 / 基尼系数 | 既看绝对水平又看分布均衡 |
| 关键词管理与运用 | **30%（最高）** | 有效率 / CPC 分布 / 跳出率 / 集中度 | 关键词是 SEM 核心生产资料 |
| 出价策略与预算 | 25% | CPC 稳定性 / 上方位渗透 / 预算 CV / 方案一致性 | 多角度量化 |
| 投放策略与时间 | 25% | 消费-注册相关性 / 周内分布 / 转化率稳定度 | 兼顾效率与稳定性 |

---

### 3.2 Prophet 反事实模拟（`q1_prophet.py`）

**最有方法论创新的部分**。

1. 两次建模对比：
   - 模型 A（带 holidays 参数）→ 含节假日效应的预测
   - 模型 B（剔除 holidays 参数）→ "假如今天不是节假日"的基线
   - 差值 A−B = 节假日净贡献
2. 把节假日分为两类：法定节假日 vs 电商购物节

**关键发现**（反直觉）：
- 法定节假日反而是抑制效应（消费 −1,735 元 / 注册 −21 人）
- 电商购物节是显著正向（双 11 +47%、99 大促 +18%）

---

### 3.3 图表产出（`q1_plots.py` + `font_fix.py`）

- 11 张图全部 **300 DPI**
- 中文显示正常（`font_fix.py` 强制注册 SimHei）
- 配色统一（五色板 `#2E86AB / #A23B72 / #F18F01 / #06A77D / #D62246`）

---

## 四、优点（8 项）

| # | 优点 | 备注 |
|---|------|------|
| A1 | 多维度评分完整 | 4×3 = 12 个二级指标覆盖 SEM 全链路 |
| A2 | 权重分配合理 | 关键词 30% 最高，符合行业认知 |
| A3 | 反事实方法新颖 | 双重 Prophet 模拟给出因果量化 |
| A4 | 反直觉发现 | 法定节假日负贡献 / 电商节正贡献 |
| A5 | 可视化专业 | 11 张图统一风格，中文清晰 |
| A6 | 工程化一键运行 | `python -m src.q1_main` 全流程复现 |
| A7 | 模块化清晰 | data_prep / scoring / prophet / plots / write 五分离 |
| A8 | 评分函数可调 | 区间 + 扣分逻辑在 `pos_score()` 等函数中可见 |

---

## 五、缺点 / 风险点 + 方案对比 + 推荐方案

### 5.1 评分权重是主观先验 ⚠️ 用户已敲定

```357:362:src/q1_scoring.py
weights = {
        '设计质量与创意':   0.20,
        '关键词管理与运用': 0.30,
        '出价策略与预算':   0.25,
        '投放策略与时间':   0.25,
    }
```

#### 5.1.1 方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **熵权法** | 数据离散度越大权重越高 | 客观 / 简单 / 可复现 | 不考虑业务重要性 / 异常值敏感 |
| **AHP** | 专家两两比较矩阵 → 特征向量权重 | 融入专家经验 / 学术常见 | 主观性强 / 一致性难保证 |
| **CRITIC** | 标准差 × 冲突性（1-r） | 同时考虑变异+相关 / 数学严谨 | 实现稍复杂 |
| **CRITIC + 业务混合**（推荐）| 0.7×CRITIC + 0.3×业务权重 | 客观+业务双驱动 / 评委最认可 | 混合比例本身需论证 |

**推荐方案 ✅**：

- **方法**：CRITIC + 业务经验**混合赋权**，权重比 **70:30**
- **理由**：
  - SEM 12 指标高度相关（"上方位占比"和"出价效率" r>0.6），CRITIC 比熵权法更适合
  - 70:30 比例保留业务可解释性，评委最认可
  - 不需要外部专家（AHP 失败的主因）
- **实现要点**：
  ```python
  # CRITIC 权重
  σ = X.std(axis=0)                              # 标准差
  conflict = (1 - np.corrcoef(X.T)).sum(axis=0)  # 冲突性
  w_critic = σ * conflict / (σ * conflict).sum()
  # 混合
  w_final = 0.7 * w_critic + 0.3 * w_subjective
  ```

---

### 5.2 评分区间划分过于死板 ⚠️ 用户已敲定

```100:107:src/q1_scoring.py
def pos_score(x, ideal_lo=0.4, ideal_hi=0.7):
        if pd.isna(x):
            return 0
        if ideal_lo <= x <= ideal_hi:
            return 100
        d = min(abs(x - ideal_lo), abs(x - ideal_hi))
        return max(0, 100 - d * 200)  # 超出区间每0.5扣100分
```

#### 5.2.1 方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **行业基准动态区间** | 引用 SEM 行业白皮书的健康值 | 有外部依据 / 评委易接受 | 本题无行业基准 / 数据获取难 |
| **模糊综合评价** | 评语集 {优/良/中/差} + 隶属度函数 | 处理模糊概念 / 符合人脑评价 | 隶属度函数本身又主观 |
| **百分位法**（P25-P75）| 用本题数据计算 P25/P75 作为合理区间 | 基于本题数据 / 自动适应分布 | "全部方案都差"时基准偏低 |
| **百分位 + Z-Score 双轨**（推荐）| 百分位定区间 + Z-Score 做得分衰减 | 同时解决区间+量纲 / 数学严谨 | 对非正态分布需 Box-Cox 变换 |

**推荐方案 ✅**：

- **方法**：百分位 P25-P75 定区间 + Z-Score 做得分衰减的**双轨制**
- **理由**：
  - 题中无行业基准数据 → 行业基准法做不了
  - 模糊综合评价引入新主观性 → 与"客观化"初衷矛盾
  - 双轨制 = 百分位法确定合理区间 + Z-Score 做得分衰减，更精细
- **实现要点**：
  ```python
  lo, hi = X.quantile(0.25), X.quantile(0.75)   # P25-P75
  z = (x - X.mean()) / X.std()                   # Z-Score
  score = 100 * max(0, 1 - max(0, abs(z) - 1))   # |z|≤1 满分, 线性衰减
  ```

---

### 5.3 Prophet 反事实置信度不足 ⚠️ 待敲定

```44:54:src/q1_prophet.py
def fit_prophet(...):
    model = Prophet(
        yearly_seasonality=True,    # ⚠️ 365天数据警告
        weekly_seasonality=True,
        ...
    )
```

**风险**：
- `yearly_seasonality=True` 会触发 Prophet 警告"训练数据不足"
- 只输出点估计 `yhat`，没有 `yhat_lower / yhat_upper`
- 反事实差值没有做显著性检验

#### 5.3.1 方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **纯区间法** | Prophet 内置 `yhat_lower/upper` 直接输出 | 一行代码 / 几乎免费 | 不检验显著性 |
| **Bootstrap 重采样** | 对原始数据有放回抽样 N 次 → N 个反事实差值分布 | 直观 / 不需模型假设 | 计算慢 / 依赖模型 |
| **贝叶斯后验** | Prophet 本身就是贝叶斯结构 / 取后验分位数 | 数学严谨 | 难解释 / 实现复杂 |
| **留一法**（LOO）| 依次剔除每个节日看结论变化 | 检验"哪个节日是关键" | 节日少时意义有限 |

**推荐方案 ✅**：

- **方法**：区间法 + Bootstrap **双轨**
  - 区间法：直接输出 `yhat_lower/upper` 作为不确定性（必做，零成本）
  - Bootstrap：1000 次重采样，给每个节日贡献计算 95% 置信区间（推荐，60min）
- **理由**：
  - 区间法是 Prophet 内置功能，先做这个免费拿一份不确定性
  - Bootstrap 能给出"假日贡献是否显著 ≠ 0"的统计判断
  - 比贝叶斯后验好解释，比留一法更定量
- **实现要点**：
  ```python
  # 1. 修正 yearly_seasonality
  model = Prophet(yearly_seasonality=False, weekly_seasonality=True, ...)
  forecast = model.predict(future)        # 自动带 yhat_lower/upper

  # 2. Bootstrap 显著性
  diffs = []
  for _ in range(1000):
      boot = df.sample(len(df), replace=True)
      m_b = fit_prophet(boot, ...)
      d_b = (predict_with_holidays(m_b) - predict_without_holidays(m_b)).mean()
      diffs.append(d_b)
  ci_low, ci_high = np.percentile(diffs, [2.5, 97.5])
  ```

---

### 5.4 评分与 Prophet 之间无定量衔接 ⚠️ 待敲定

**风险**：
- 5.1.4 节指出"6 月低谷"得分低，但没定量说"6 月比均值低多少"
- 5.1.5 节给出假日贡献绝对值，但没在评分中体现

#### 5.4.1 方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **直接扣分** | 节假日贡献差值（负数）→ 直接扣 X 分 | 实现最简单 | 容易"扣分爆炸" |
| **分级扣分**（推荐）| 按显著性分级（>0/0.05/0.10 三档）扣分 | 与显著性挂钩 / 严谨 | 需要先做 5.3 显著性 |
| **独立加分维度** | 新增一维"时间布局"（占 5%） | 不污染原 4 维结构 | 4 维变 5 维，权重需重定 |
| **风险预警标注** | 不扣分，只在评分旁加 ⚠️ 标签 | 不破坏现有评分 | 没定量影响评分 |

**推荐方案 ✅**：

- **方法**：分级扣分（与 5.3 Bootstrap 显著性挂钩）
- **理由**：
  - 直接扣分法可能因为"差值绝对值大"导致评分暴跌（如某节假日贡献 -5000 元 → 扣 100 分不合理）
  - 独立加分维度需要重定 5 个维度的权重，影响太大
  - 风险预警标注不够定量，论文里评委看不到
  - 分级扣分与显著性挂钩，能体现"节日布局错位的严重程度"
- **实现要点**：
  ```python
  # 在"投放策略与时间"维度下加一个扣分项
  if p_value < 0.05:       # 显著错位
      time_score -= 15
  elif p_value < 0.10:     # 边缘显著
      time_score -= 8
  elif p_value < 0.20:     # 弱信号
      time_score -= 3
  ```

---

### 5.5 跳出率指标是 Sheet3 全年汇总 ⚠️ 待敲定

**风险**：
- Sheet3 是关键词**年度聚合**，不是日级
- 无法做"关键词跳出率随时间的演化分析"
- 无法定位"哪个节假日期间跳出率突然升高"

#### 5.5.1 方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **方案A：ID 关联反推** | Sheet1 (campaign) + Sheet3 (keyword) 关联 → 模拟"逐日近似" | 利用现有全部数据 | 近似而非真实（campaign 日数据无跳出率） |
| **方案B：关键词聚类** | 按跳出率聚类（KMeans），找规律 | 揭示"高跳出率关键词特征" | 仍是年度静态 |
| **方案C：时间窗滑动平均** | 假设跳出率在月内平稳 → 14 天滑窗 | 显示"准时间演化" | 假设过强 |
| **方案D：静态披露**（推荐） | 不强行做演化，只在论文说明"数据局限" | 诚实 / 不引入错误结论 | 信息量较少 |

**推荐方案 ✅**：

- **方法**：**静态披露（方案D）+ 关键词聚类（方案B）**
  - D 是必做的：明确说明"跳出率为年度静态值，无法做时间演化"
  - B 是补充做的：在静态基础上做聚类，揭示"高跳出率关键词的共同特征"
- **理由**：
  - 方案 A 的"反推"会引入错误因果（campaign 日数据无跳出率，强行关联是欺骗）
  - 方案 C 的滑窗假设过强
  - 方案 B + D 组合既诚实又有信息量
- **实现要点**：
  ```python
  # B: 关键词聚类
  from sklearn.cluster import KMeans
  X = df[['跳出率', 'CPC', '点击量']].fillna(0)
  km = KMeans(n_clusters=4).fit(X)
  df['簇'] = km.labels_
  # 论文说明
  print("跳出率为年度静态值，受数据可得性限制不做时间演化分析")
  ```

---

### 5.6 缺乏稳健性 / 敏感性分析 ⚠️ 待敲定

**风险**：
- 评分权重 ±5% 变动时最终评分跳多少？
- 反事实剔除不同节假日时结论稳定吗？
- 异常值（如 8 月 22 万元的高消费日）是否影响了趋势拟合？

#### 5.6.1 方案对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **权重扰动法** | 对权重 ±20% 扰动，观察最终评分极差 | 极简单 / 解释直接 | 单维度敏感性 |
| **蒙特卡洛模拟** | 10000 次随机采样 → 评分分布 | 全维度 / 给出置信区间 | 计算慢 / 结果难解释 |
| **龙卷风图**（推荐）| 一元敏感性分析 → 龙卷风图（Tornado Chart） | 视觉直观 / 评委最爱 | 单因子扫描 |
| **多场景对比** | 设 3-4 套不同权重组合 → 全套评分 | 完整 / 易写 | 工作量大 |

**推荐方案 ✅**：

- **方法**：**权重扰动法 + 龙卷风图**
- **理由**：
  - 蒙特卡洛对本题"样本数 = 方案数"（N=4-8）意义不大
  - 龙卷风图评委最认可——"哪个指标对最终评分影响最大"
  - 多场景对比工作量大且重复
  - 权重扰动法 + 龙卷风图组合：扰动产生数据 → 龙卷风图可视化 → 评委一眼明白
- **实现要点**：
  ```python
  sensitivities = []
  for w in weights_candidates:
      for delta in np.arange(-0.20, 0.21, 0.05):
          w_perturbed = w * (1 + delta)
          score_perturbed = compute_score(w_perturbed)
          sensitivities.append((indicator, delta, score_perturbed))
  # 画龙卷风图：横轴是评分变化，纵轴是指标
  ```

---

## 六、自评打分

| 评价项 | 评分 | 备注 |
|-------|------|------|
| 方法完整性 | 8.5 / 10 | 数据→评分→反事实→图表→文字 全闭环 |
| 方法新颖性 | 9.0 / 10 | Prophet 反事实 + 评分雷达是亮点 |
| 结果说服力 | 8.0 / 10 | 数据支撑充分，反事实结论有商业价值 |
| 可视化质量 | 9.0 / 10 | 11 张图全部专业、中文清晰、配色统一 |
| **学术严谨性** | **5.5 / 10** | 缺显著性检验、权重主观、稳健性分析 |
| 工程化程度 | 9.0 / 10 | 一键运行、模块化、可复现 |
| **综合** | **8.0 / 10** | 论文提交级别，缺稳健性章节会扣印象分 |

---

## 七、改进路线图（已敲定 + 待敲定）

### ✅ 已敲定（5.1, 5.2）

| 编号 | 任务 | 预计耗时 |
|------|------|---------|
| 改-1 | CRITIC + 业务经验混合赋权（70:30） | 30 min |
| 改-2 | 百分位 + Z-Score 双轨区间 | 30 min |

### ⚠️ 待敲定（5.3, 5.4, 5.5, 5.6）

| 编号 | 任务 | 预计耗时 | 难度 | 推荐方案 |
|------|------|---------|------|---------|
| 改-3 | Prophet `yearly=False` + 置信区间 | 20 min | ★ | 区间法（必做） |
| 改-4 | Bootstrap 检验节日贡献显著性 | 60 min | ★★★ | Bootstrap（推荐） |
| 改-5 | 节日贡献注入评分（分级扣分） | 30 min | ★★ | 分级扣分（推荐） |
| 改-6 | 跳出率静态披露 + 关键词聚类 | 30 min | ★★ | D + B（推荐） |
| 改-7 | 权重扰动 + 龙卷风图敏感性分析 | 30 min | ★★ | 权重扰动+龙卷风（推荐）|

---

## 八、待用户敲定事项汇总

请逐项确认或修改推荐方案：

| 决策点 | 推荐方案 | 备选方案 | 备注 |
|--------|---------|---------|------|
| 5.1 权重法 | ✅ CRITIC + 业务混合 70:30 | 纯 CRITIC / 纯熵权 | **已敲定** |
| 5.2 区间法 | ✅ 百分位 + Z-Score 双轨 | 行业基准 / 模糊综合 | **已敲定** |
| 5.3 Prophet 置信度 | **区间法 + Bootstrap 双轨** | 仅区间 / 贝叶斯 / 留一 | 待敲定 |
| 5.4 节日贡献注入评分 | **分级扣分** | 直接扣分 / 独立维度 / 预警 | 待敲定 |
| 5.5 跳出率时间演化 | **静态披露 + 关键词聚类** | ID 关联反推 / 时间窗 | 待敲定 |
| 5.6 稳健性分析 | **权重扰动 + 龙卷风图** | 蒙特卡洛 / 多场景 | 待敲定 |

---

## 九、执行计划（待用户拍板后启动）

确认后将按以下顺序执行，并在每次完成后更新文档：

1. 改-1（CRITIC 赋权）→ 输出新评分表 + 对比表
2. 改-2（百分位+Z-Score）→ 输出新评分函数 + 12 指标新得分
3. 改-3（Prophet 修正）→ 重跑 Prophet + 输出置信区间图
4. 改-7（敏感性分析）→ 输出龙卷风图 + 权重稳健性报告
5. 改-4（Bootstrap）→ 输出显著性结果 + 节日贡献 p 值表
6. 改-5（节日扣分注入）→ 更新最终评分
7. 改-6（跳出率聚类）→ 输出聚类结果图 + 论文说明
8. 同步更新 `paper/q1_section_5_1.md` 文字稿
