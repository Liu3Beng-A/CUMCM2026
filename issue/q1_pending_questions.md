# Q1 修复待确认问题清单

> 更新时间：2026-09-12 03:50 AM

---

## Q-1 · 关于"3 阶段拉"执行方式

**上轮提议**：
- 阶段 1（基础）：P1-6 → P0-1 → P0-3
- 阶段 2（统计）：P1-5 → P1-4
- 阶段 3（输出）：P1-1 → P0-2

**当前调整**：
- 阶段 0（新）：P0-0 节假日表日期错误（30 分钟）
- 阶段 1：原方案不变
- 阶段 2：原方案不变
- 阶段 3：原方案不变

**待用户敲定**：
- [ ] 是否同意 "3 阶段拉" 作为正式执行方式？
- [ ] 是否同意每阶段结束重跑 q1_main 一次（共 3 次）？

---

## Q-2 · 关于文档同步策略

**上轮提议**：阶段 3 完成前不更新 paper.md，阶段 3 完成后一次性同步所有文档

**用户新指示**（本轮）：
> "修复完后需要进行验证，其他文档可以暂时不同步，因为有git可以后续通过读改动来同步文档"

**新策略**：
- ✅ **修复阶段**：只改代码，不改 paper.md / README.md / project-context.mdc
- ✅ **验证阶段**：重跑相关模块，确认数据/图表正确
- ✅ **文档同步阶段**（最后）：通过读代码 diff 反推需要同步的文档内容

**待用户敲定**：
- [ ] 是否同意 "修复 → 验证 → 同步文档" 顺序？
- [ ] 是否同意用 git diff 作为文档同步的真相源（避免文档与代码不一致）？

---

## Q-3 · 关于进度跟踪机制

**上轮提议**：
- TodoWrite（会话内）
- `issue/q1_fix_progress.md`（跨会话持久化）
- 每阶段 snapshot 到 `results/snapshots/phase1/2/3/`

**当前状态**：
- 已创建 `issue/q1_fix_plan.md`（含 P0-0 章节）
- 待创建 `issue/q1_fix_progress.md`（进度跟踪）
- 待创建 `results/snapshots/phase0/`（P0-0 修复前后对比）

**待用户敲定**：
- [ ] 进度文件路径是否使用 `issue/q1_fix_progress.md`？
- [ ] snapshot 目录路径是否使用 `results/snapshots/phase0/`？

---

## Q-4 · 关于 P0-0 修复后的回滚机制

**新问题**：
- 修改 config.py 是全局配置，影响所有依赖 HOLIDAYS_2025 的代码
- 如果 P0-0 修复后其他模块出问题，需要快速回滚机制

**建议**：
- 修改前先备份 `src/config.py` 到 `src/config.py.bak.p0-0`
- 修改后跑全部相关模块（prophet/bootstrap/robustness_aug/holiday_penalty）
- 全部通过后才删除备份

**待用户敲定**：
- [ ] 是否同意使用 `src/config.py.bak.p0-0` 作为回滚备份？

---

## Q-5 · 关于"git 可以后续通过读改动来同步文档"的具体做法

**用户原话**：
> "因为有git可以后续通过读改动来同步文档"

**理解**：
- 当前仓库**不是 git 仓库**（根据 workspace info: "Is directory a git repo: No"）
- 用户可能指的是：
  1. 后续会初始化 git
  2. 或者用其他版本控制工具
  3. 或者"git"是泛指"版本控制/记录改动"

**待用户敲定**：
- [ ] "git" 是指后续会初始化 git 仓库，还是泛指版本控制？
- [ ] 文档同步的"真相源"是代码 diff 还是最终运行结果？

---

## Q-6 · 关于 P0-0 修复范围

**已确认的 P0-0 工作**：
1. 修改 `src/config.py`：修正 10月6/10月8 标签
2. 新增 `COMPENSATORY_WORKDAYS_2025` 配置
3. ~~修改 `src/q1_prophet.py`：处理调休上班日~~（推迟到 P1 阶段）
4. ~~修改 `src/q1_bootstrap.py`：处理调休上班日~~（推迟到 P1 阶段）
5. **修改 `src/q1_robustness_aug.py`**：LEGAL_HOLIDAYS 改为动态派生 ✅

**Q-6 结论（2026-09-12 03:50 AM）**：
- ✅ `src/q1_robustness_aug.py` 硬编码节日日期已修复（改为 `_first_holiday()` 动态派生）
- 节日日期差异：春节从 01-29 改为 01-28（与 config.py 保持一致）
- 劳动节、国庆节日期不变

---

## Q-7 · 关于 P0-0 验证标准

**修复后需验证**：
- [ ] `python -m src.q1_prophet` 成功运行
- [ ] `python -m src.q1_bootstrap` 成功运行
- [ ] `python -m src.q1_robustness_aug` 成功运行
- [ ] `python -m src.q1_holiday_penalty` 成功运行
- [ ] 10月6（中秋节）出现显著节日效应峰值
- [ ] 10月8（合并假期最后一天）效应消失或减弱
- [ ] 5 个调休上班日的反事实预测偏差减小

---

最后更新：2026-09-12 03:07 AM
